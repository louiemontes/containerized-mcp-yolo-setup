services:
  claude-code:
    build: .
    stdin_open: true
    tty: true
    environment:
      - ANTHROPIC_API_KEY
      - AGENT_GITHUB_TOKEN
    volumes:
      - ./workspace:/workspace
    working_dir: /workspace
    command: tail -f /dev/null
