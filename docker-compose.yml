services:
  claude-code:
    build: .
    stdin_open: true
    tty: true
    environment:
      - ANTHROPIC_API_KEY
    volumes:
      - ./workspace:/workspace
    working_dir: /workspace
    command: tail -f /dev/null
